他问熟悉什么语言啊，于是我果断就说C++了。。。。。。
然后纯C++语法。。。。。。一个算法也没问。
问的题的顺序大概就下面这样：

1. 定义一个宏，给两个数，返回其中较大的
2. 含有10个元素的指针数组
3. 一个函数指针，带有一个int型参数，返回类型为int
4. 常量指针；指向常量的指针
5. const修饰类方法时的作用
6. 虚函数怎么实现的，编译器怎么处理
7. 析构函数能设为虚函数么？为什么要弄成虚函数？
8. 实现C语言库函数strcpy：char* strcpy(char *dest, const char *src);
9. 给一个单链表，有两个指针分别指向链表中两个连续节点A和B，交换这两个节点.
10. malloc分配内存时会传一个分配容量的参数，但free时只传了分配的内存的首地址，具体是怎么free这部分分配的内存的.
11. 进程和线程的区别？CPU调度单位？单CPU中的多任务怎么实现的？
12. struct和class的区别
13. 实现动态数组

最后问了个项目的问题。略了
还有他也问了什么觉得哪门课学的好啊，还有印象么之类的。。。。我说了数据结构，说完他就问了实现个动态数组。。。问操作系统有印象么，有点吧，然后进程和线程的区别。。问知道内存怎么分配么。。。不知道。。。然后问malloc那个

下面是我回答的。。。
----------------------------
1. #define MAX(x, y) (((x)>(y)) ? (x) : (y))
2. int *p[10]
3. int (*f)(int)
4. int * const p; const int *p
5. 方法里不能修改类成员
6. 虚函数表
7. 基类指针指向了派生类等等之类的
8. 
char* strcpy(char *dest, const char *src)
{
    if (NULL==dest || NULL==src)
    {
        return NULL;
    }

    // 我记得这里是要用个备份, dest拷贝了之后都不是传进来的时候的地址了
    // 我写了之后那面试官说可以不用，但是应该还是要的
    char *tmp = dest;
    while ((*dest++ = *src++) != '\0')
    {}
    
    return tmp;
}
9. 我先说了个从头找到A的前驱，然后改指针指向。之后问他直接交换节点吧，他说节点内容多的时候就不好交换了。。。
10. 不知，他说分配的地址前面保存了容量信息，free的时候会读取到这个信息
11. Google之
12. struct默认public, class默认private
13. 我跟他说类似vector那样，设定个capacity和size，capacity比size大，每次容量不够了，增大capacity.

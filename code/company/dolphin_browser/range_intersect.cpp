
struct range
{
    float start;
    float end;
    range(float _start, float _end) : start(_start), end(_end)
    {}
};

bool is_range_intersected(const list<range> &ranges, const range &rg)
{
    for (list<range>::const_iterator )
}
#include <iostream>
#include <queue>
#include <stack>
using std::cout;
using std::queue;
using std::stack;

bool is_pop_order(const int *push, const int *pop, int length)
{
    queue<int> push_order;
}